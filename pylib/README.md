This directory contains the *libraries* in the `lib` folder ported to python with the help of `ctypes` module.

### Table Of Content

- [trig library](trig)


This is purerly exist here because I want to make fancy animations with `manim`;
